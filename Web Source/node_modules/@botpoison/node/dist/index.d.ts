declare type BotpoisonOptions = {
    secretKey: string;
};
declare type VerificationResponse = {
    ok: boolean;
};
declare class Botpoison {
    private readonly secretKey;
    constructor({ secretKey }: BotpoisonOptions);
    verify(solution: string): Promise<VerificationResponse>;
}
export = Botpoison;
